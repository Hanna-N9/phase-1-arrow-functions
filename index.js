//Function declaration
function divide(a, b) {
  return a / b;
}

//Arrow function with a parameter and multiplies it times itself
const square = x => x * x;

//Arrow function with two parameters, adds them together
const add = (a, b) => {
  return a + b;
};
